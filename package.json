{
  "name": "pm2promises",
  "version": "0.9.5",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "AGPL-3.0",
  "scripts": {
    "lint": "yarn run tslint --config tslint.json --project tsconfig.json",
    "build": "yarn run tsc --project tsconfig.json",
    "test": "yarn run jest --config jest.config.json --projects tsconfig.jest.json --detectOpenHandles --no-cache",
    "all": "yarn run lint && yarn run build && yarn run test",
    "prepublish": "yarn all"
  },
  "dependencies": {
    "axon": "^2.0.3",
    "pm2": "^3.1.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mafh612/pm2promise"
  },
  "author": {
    "name": "mafh",
    "url": "https://github.com/mafh612"
  },
  "devDependencies": {
    "@types/axon": "^2.0.0",
    "@types/jest": "^23.3.2",
    "@types/node": "^10.10.3",
    "jest": "^23.6.0",
    "ts-jest": "^23.10.1",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  },
  "keywords": [
    "pm2",
    "promise"
  ]
}
